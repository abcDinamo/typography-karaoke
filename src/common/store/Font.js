class Font {
  constructor(id, name, description, url, color) {
    this.id = id;
    this.name = name;
    this.description = description;
    this.url = url;
    this.color = color;
  }
}

export default Font;
